const http = require('http');
const fs = require('fs');
const path = require('path');
const { execFile } = require('child_process');
const { promisify } = require('util');
const { Mark2Runtime } = require('./mark2-runtime');
const { config } = require('./config');
const { snapshot } = require('./inspector');
const { execute, listTools } = require('./executor');
const voice = require('./voice');
const { getVoiceModel } = require('./voice/fish-model');
const { maintenanceSnapshot, heal } = require('./maintenance-api');
const { startVoiceDaemon, stopVoiceDaemon, voiceDaemonStatus } = require('./voice/daemon');
const voiceConfig = require('./voice/config').config;
const { collectSystemStatus } = require('./status/system-status');
const credentialStore = require('./credentials/local-store');
const execFileAsync = promisify(execFile);
const core = new Mark2Runtime();
const legacyUiFile = path.resolve(__dirname, '..', 'interface-test', 'index.html');
const interfaceDistRoot = path.resolve(__dirname, '..', 'interface', 'dist');
const audioRoot = path.resolve(voiceConfig.outputDir);

function send(res,status,payload,contentType='application/json; charset=utf-8'){const body=typeof payload==='string'||payload instanceof Buffer?payload:JSON.stringify(payload);res.writeHead(status,{'Content-Type':contentType,'Content-Length':Buffer.byteLength(body),'Cache-Control':'no-store','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization','Access-Control-Allow-Methods':'GET, POST, OPTIONS'});res.end(body);}
function readBody(req){return new Promise((resolve,reject)=>{let raw='';req.setEncoding('utf8');req.on('data',c=>{raw+=c;if(raw.length>100000)reject(new Error('Request body too large.'));});req.on('end',()=>resolve(raw));req.on('error',reject);});}
function mimeType(file){const ext=path.extname(file).toLowerCase();return{'.html':'text/html; charset=utf-8','.js':'application/javascript; charset=utf-8','.mjs':'application/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.svg':'image/svg+xml','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.webp':'image/webp','.ico':'image/x-icon','.woff':'font/woff','.woff2':'font/woff2','.txt':'text/plain; charset=utf-8'}[ext]||'application/octet-stream';}
function safeInterfacePath(requestPath){const pathname=new URL(requestPath,'http://127.0.0.1').pathname;const relative=pathname.replace(/^\/+/, '');const candidate=path.resolve(interfaceDistRoot,relative);if(!candidate.startsWith(interfaceDistRoot+path.sep)&&candidate!==interfaceDistRoot)return null;return candidate;}
function serveIntegratedInterface(req,res){const candidate=safeInterfacePath(req.url||'/');if(candidate&&fs.existsSync(candidate)&&fs.statSync(candidate).isFile()){const stat=fs.statSync(candidate);res.writeHead(200,{'Content-Type':mimeType(candidate),'Content-Length':stat.size,'Cache-Control':candidate.includes(`${path.sep}assets${path.sep}`)?'public, max-age=31536000, immutable':'no-cache'});fs.createReadStream(candidate).pipe(res);return true;}const indexFile=path.join(interfaceDistRoot,'index.html');if(fs.existsSync(indexFile)){const stat=fs.statSync(indexFile);res.writeHead(200,{'Content-Type':'text/html; charset=utf-8','Content-Length':stat.size,'Cache-Control':'no-cache'});fs.createReadStream(indexFile).pipe(res);return true;}return false;}
async function playLocalAudio(file){const resolved=path.resolve(file);if(!resolved.startsWith(audioRoot+path.sep)||!fs.existsSync(resolved))throw new Error('Audio file not found.');if(process.platform!=='win32')return{ok:false,error:'Local direct playback currently targets Windows.'};const escaped=resolved.replace(/'/g,"''");const ps=`$p = New-Object System.Windows.Media.MediaPlayer; $p.Open([Uri]::new('${escaped}')); Start-Sleep -Milliseconds 500; $p.Play(); while ($p.NaturalDuration.HasTimeSpan -eq $false) { Start-Sleep -Milliseconds 100 }; Start-Sleep -Milliseconds ([int]$p.NaturalDuration.TimeSpan.TotalMilliseconds + 250); $p.Close()`;await execFileAsync('powershell.exe',['-NoProfile','-NonInteractive','-Command','Add-Type -AssemblyName PresentationCore; '+ps],{timeout:120000,windowsHide:true,maxBuffer:1024*1024});return{ok:true,played:true,path:resolved};}
function allowedCredentialKeys(body){const allowed=['GITHUB_TOKEN','GH_TOKEN','INSTAGRAM_ACCESS_TOKEN','INSTAGRAM_USER_ID','IG_ACCESS_TOKEN','IG_USER_ID','OMNIROUTE_API_KEY','FISH_API_KEY','GEMINI_API_KEY','GOOGLE_API_KEY','OPENAI_API_KEY','ANTHROPIC_API_KEY','DEEPSEEK_API_KEY','GROQ_API_KEY','MISTRAL_API_KEY','XAI_API_KEY','OPENROUTER_API_KEY','OPENCODE_API_KEY','OPENCODE_GO_API_KEY'];return Object.fromEntries(Object.entries(body||{}).filter(([key,value])=>allowed.includes(key)&&value!=null&&String(value)!=='').map(([key,value])=>[key,String(value)]));}
function writeSse(res,type,payload){if(res.writableEnded)return;res.write(`event: ${type}\ndata: ${JSON.stringify(payload)}\n\n`);}
async function handleChatStream(req,res){let closed=false;req.on('close',()=>{closed=true;});res.writeHead(200,{'Content-Type':'text/event-stream; charset=utf-8','Cache-Control':'no-cache, no-transform','Connection':'keep-alive','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization','Access-Control-Allow-Methods':'GET, POST, OPTIONS','X-Accel-Buffering':'no'});res.flushHeaders?.();try{const body=JSON.parse((await readBody(req))||'{}');if(!String(body.message||'').trim()){writeSse(res,'error',{error:'Message is required.'});return;}await core.handleMessageStream(body.message,{confirmed:body.confirmed===true,model:body.model,action:body.action||null,source:body.source||'interface'},event=>{if(!closed)writeSse(res,event.type,event);});}catch(error){if(!closed)writeSse(res,'error',{error:error?.message||String(error),status:error?.status||null});}finally{if(!res.writableEnded)res.end();}}
const server=http.createServer(async(req,res)=>{try{if(req.method==='OPTIONS')return send(res,204,'');if(req.method==='GET'&&req.url==='/health')return send(res,200,{ok:true,service:'ultron-core',...core.status(),runtime:'mark2-unified'});if(req.method==='GET'&&req.url.startsWith('/api/audio?')){const q=new URL(req.url,'http://127.0.0.1').searchParams;const requested=path.basename(q.get('path')||'');if(!requested)return send(res,400,{ok:false,error:'Audio path is required.'});const file=path.resolve(audioRoot,requested);if(!file.startsWith(audioRoot+path.sep)||!fs.existsSync(file))return send(res,404,{ok:false,error:'Audio file not found.'});const type=mimeType(file),stat=fs.statSync(file);res.writeHead(200,{'Content-Type':type,'Content-Length':stat.size,'Cache-Control':'no-store','Accept-Ranges':'bytes'});return fs.createReadStream(file).pipe(res);}if(req.method==='GET'&&req.url==='/api/status')return send(res,200,await collectSystemStatus());if(req.method==='GET'&&req.url==='/api/status/mood')return send(res,200,(await collectSystemStatus()).status.mood);if(req.method==='GET'&&req.url==='/api/status/github')return send(res,200,(await collectSystemStatus()).status.github);if(req.method==='GET'&&req.url==='/api/status/instagram')return send(res,200,(await collectSystemStatus()).status.instagram);if(req.method==='GET'&&req.url==='/api/status/administrator')return send(res,200,(await collectSystemStatus()).status.administrator);if(req.method==='GET'&&req.url==='/api/status/omniroute')return send(res,200,(await collectSystemStatus()).status.omniroute);if(req.method==='GET'&&req.url==='/api/status/internet-speed')return send(res,200,(await collectSystemStatus()).status.internetSpeed);if(req.method==='GET'&&req.url==='/api/status/memory')return send(res,200,(await collectSystemStatus()).status.memory);if(req.method==='GET'&&req.url==='/api/credentials/status')return send(res,200,await credentialStore.status());if(req.method==='POST'&&req.url==='/api/credentials'){const body=JSON.parse((await readBody(req))||'{}');const values=allowedCredentialKeys(body);if(!Object.keys(values).length)return send(res,400,{ok:false,error:'No supported credential fields were supplied.'});return send(res,200,{ok:true,...(await credentialStore.setMany(values))});}if(req.method==='GET'&&req.url==='/api/memory')return send(res,200,{ok:true,memories:await core.getMemories()});if(req.method==='POST'&&req.url==='/api/memory'){const body=JSON.parse((await readBody(req))||'{}');const key=String(body.key||'').trim(),value=String(body.value||'').trim();if(!key||!value)return send(res,400,{ok:false,error:'Memory key and value are required.'});const result=await core.rememberCandidate({key,value,category:body.category||'user',source:'interface'});return send(res,200,{ok:true,result,memories:await core.getMemories()});}if(req.method==='GET'&&req.url==='/api/tools')return send(res,200,{ok:true,tools:listTools()});if(req.method==='GET'&&req.url==='/api/inspect')return send(res,200,{ok:true,...(await snapshot(core))});if(req.method==='GET'&&req.url==='/api/maintenance')return send(res,200,{ok:true,...(await maintenanceSnapshot(core))});if(req.method==='POST'&&req.url==='/api/self-heal')return send(res,200,await heal());if(req.method==='GET'&&req.url==='/api/voice/daemon')return send(res,200,{ok:true,...voiceDaemonStatus()});if(req.method==='POST'&&req.url==='/api/voice/daemon/start')return send(res,200,await startVoiceDaemon());if(req.method==='POST'&&req.url==='/api/voice/daemon/stop')return send(res,200,stopVoiceDaemon());if(req.method==='GET'&&req.url==='/api/voice/model'){try{const model=await getVoiceModel();return send(res,200,{ok:true,configured_reference_id:voiceConfig.referenceId,model:{id:model?._id,title:model?.title,type:model?.type,state:model?.state,visibility:model?.visibility,author:model?.author?.nickname,description:model?.description}});}catch(error){return send(res,502,{ok:false,configured_reference_id:voiceConfig.referenceId,error:error.message});}}if(req.method==='POST'&&req.url==='/api/tools/execute'){const body=JSON.parse((await readBody(req))||'{}');if(!body.name)return send(res,400,{ok:false,error:'Tool name is required.'});const result=await execute(body.name,body.input||{},{confirmed:body.confirmed===true,source:body.source||'interface'});return send(res,result.ok?200:result.requires_confirmation?409:400,result);}if(req.method==='POST'&&req.url==='/api/tts'){const body=JSON.parse((await readBody(req))||'{}');if(!String(body.text||'').trim())return send(res,400,{ok:false,error:'Text is required.'});return send(res,200,await voice.synthesize(body.text,{filename:body.filename,model:body.model,referenceId:body.reference_id,format:body.format}));}if(req.method==='POST'&&req.url==='/api/tts/play'){const body=JSON.parse((await readBody(req))||'{}');if(!String(body.text||'').trim())return send(res,400,{ok:false,error:'Text is required.'});const audio=await voice.synthesize(body.text,{filename:body.filename,model:body.model,referenceId:body.reference_id,format:body.format});return send(res,200,{...audio,playback:await playLocalAudio(audio.path)});}if(req.method==='POST'&&req.url==='/api/audio/play'){const body=JSON.parse((await readBody(req))||'{}');return send(res,200,await playLocalAudio(body.path));}if(req.method==='GET'&&req.url==='/api/voice/status')return send(res,200,{ok:true,...voice.status(),daemon:voiceDaemonStatus()});if(req.method==='POST'&&req.url==='/api/chat/stream')return handleChatStream(req,res);if(req.method==='POST'&&req.url==='/api/chat'){const body=JSON.parse((await readBody(req))||'{}');return send(res,200,await core.handleMessage(body.message,{confirmed:body.confirmed===true,model:body.model,action:body.action||null,source:body.source||'interface'}));}if(req.method==='GET'){if(serveIntegratedInterface(req,res))return;if((req.url==='/test-ui'||req.url==='/legacy-test-ui')&&fs.existsSync(legacyUiFile))return send(res,200,fs.readFileSync(legacyUiFile,'utf8'),'text/html; charset=utf-8');}return send(res,404,{ok:false,error:'Not found.'});}catch(error){if(!res.headersSent)return send(res,500,{ok:false,error:error?.message||String(error)});try{res.end();}catch{}}});
server.listen(config.port,config.host,()=>console.log(`ULTRON Core listening at http://${config.host}:${config.port}`));
