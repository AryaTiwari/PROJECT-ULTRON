const fs = require('fs');
const path = require('path');
const credentialStore = require('../credentials/local-store');
const { config } = require('./config');
const { processMetallic } = require('./metallic-postprocess');
const MODEL = process.env.ULTRON_FISH_MODEL || 's2.1-pro-free';
async function getApiKey() { const envKey = String(process.env.FISH_API_KEY || '').trim(); if (envKey) return envKey; try { const stored = await credentialStore.load(); return String(stored.FISH_API_KEY || '').trim(); } catch { return ''; } }
function statePath() { return path.resolve(process.env.ULTRON_VOICE_CLONE_STATE || '.ultron/voice/voice-clone.json'); }
function readState() { try { return JSON.parse(fs.readFileSync(statePath(), 'utf8')); } catch { return {}; } }
async function client() { const apiKey = await getApiKey(); if (!apiKey) throw new Error('FISH_API_KEY is not configured. Add a free Fish Audio developer key to .env or ULTRON credentials.'); const mod = await import('fish-audio'); return { client: new mod.FishAudioClient({ apiKey }), apiKey, mod }; }
function cleanSpeechText(text) { let value = String(text || '').normalize('NFKC'); value = value.replace(/```[\s\S]*?```/g, ' '); value = value.replace(/https?:\/\/\S+/gi, ' link '); value = value.replace(/www\.\S+/gi, ' link '); value = value.replace(/&/g, ' and '); value = value.replace(/%/g, ' percent '); value = value.replace(/\$/g, ' dollars '); value = value.replace(/#/g, ' number '); value = value.replace(/\+/g, ' plus '); value = value.replace(/[*_~^`|\\=[\]{}<>/]/g, ' '); value = value.replace(/[^\p{L}\p{N}\s.,!?;:'"()-]/gu, ' '); value = value.replace(/([.!?,;:])\1+/g, '$1'); value = value.replace(/\s+([,!?;:])/g, '$1'); value = value.replace(/([,;:])(?=\S)/g, '$1 '); value = value.replace(/\s+/g, ' ').trim(); return value.slice(0, 4096); }
async function cloneVoice(options = {}) { const { client: fish } = await client(); const reference = path.resolve(options.referencePath || config.referencePath); if (!fs.existsSync(reference)) throw new Error(`Voice reference not found: ${reference}`); const audioFile = fs.createReadStream(reference); const title = options.title || 'ULTRON Voice'; const response = await fish.voices.ivc.create({ title, voices: [audioFile] }); const state = readState(); state.provider = 'fish-audio-s2.1-pro-free'; state.engine = 'fish-audio'; state.model = MODEL; state.referencePath = reference; state.voiceProfileReady = true; state.fishVoiceId = response._id; state.fishVoiceState = response.state; fs.mkdirSync(path.dirname(statePath()), { recursive: true }); fs.writeFileSync(statePath(), JSON.stringify(state, null, 2), 'utf8'); return { ok: true, voiceId: response._id, state: response.state, title: response.title }; }
async function streamToBuffer(value) { if (Buffer.isBuffer(value)) return value; if (value instanceof Uint8Array) return Buffer.from(value); if (value instanceof ArrayBuffer) return Buffer.from(value); if (value && typeof value.arrayBuffer === 'function') return Buffer.from(await value.arrayBuffer()); if (value && typeof value.getReader === 'function') { const reader = value.getReader(); const chunks = []; while (true) { const { done, value: chunk } = await reader.read(); if (done) break; if (chunk) chunks.push(Buffer.from(chunk)); } return Buffer.concat(chunks); } if (value && typeof value.on === 'function') return await new Promise((resolve, reject) => { const chunks = []; value.on('data', chunk => chunks.push(Buffer.from(chunk))); value.on('end', () => resolve(Buffer.concat(chunks))); value.on('error', reject); }); throw new TypeError(`Unsupported Fish Audio response type: ${Object.prototype.toString.call(value)}`); }
function resolveProsody(text, options) { const end = String(text || '').trim().slice(-2); const expressive = /[!?]$/.test(end); const question = /\?$/.test(end); const configured = options.prosody && typeof options.prosody === 'object' ? options.prosody : {}; return { speed: Math.max(0.5, Math.min(2, Number(configured.speed ?? options.speed ?? (question ? 1.0 : expressive ? 1.01 : 0.98)))), volume: Math.max(-20, Math.min(20, Number(configured.volume ?? options.volume ?? 6))), normalize_loudness: true }; }
async function synthesize(text, options = {}) { const input = cleanSpeechText(text); if (!input) throw new Error('TTS text became empty after speech cleanup.'); const { client: fish } = await client(); const state = readState(); const voiceId = String(options.referenceId || state.fishVoiceId || process.env.ULTRON_FISH_REFERENCE_ID || '').trim(); if (!voiceId) throw new Error('ULTRON Fish voice clone is not configured. Run npm run core:voice-setup with FISH_API_KEY available.'); const prosody = resolveProsody(input, options); const audio = await fish.textToSpeech.convert({ text: input, reference_id: voiceId, format: options.format || 'mp3', latency: options.latency || 'balanced', chunk_length: Math.max(100, Math.min(300, Number(options.chunkLength || 300))), normalize: true, prosody, temperature: Math.max(0, Math.min(1, Number(options.temperature ?? 0.74))), top_p: Math.max(0, Math.min(1, Number(options.topP ?? 0.78))), max_new_tokens: Number(options.maxNewTokens || 1024), repetition_penalty: Number(options.repetitionPenalty || 1.2), min_chunk_length: Math.max(0, Math.min(100, Number(options.minChunkLength || 50))), condition_on_previous_chunks: options.conditionOnPreviousChunks !== false, early_stop_threshold: Number(options.earlyStopThreshold ?? 1) }, MODEL); const outputDir = path.resolve(options.outputDir || config.outputDir); fs.mkdirSync(outputDir, { recursive: true }); const ext = (options.format || 'mp3').toLowerCase() === 'wav' ? 'wav' : 'mp3'; const outputPath = path.resolve(outputDir, options.filename || `ultron-${Date.now()}.${ext}`); const buffer = await streamToBuffer(audio); fs.writeFileSync(outputPath, buffer); const processed = options.metallic === false ? { path: outputPath, applied: false } : await processMetallic(outputPath, outputPath); const finalPath = processed.path || outputPath; return { ok: true, provider: 'fish-audio-s2.1-pro-free', model: MODEL, referenceId: voiceId, referencePath: config.referencePath, path: finalPath, bytes: fs.statSync(finalPath).size, metallicApplied: processed.applied, metallicMix: options.metallic === false ? 0 : config.metallicMix, prosody }; }
module.exports = { synthesize, cloneVoice, getApiKey, readState, MODEL, streamToBuffer, cleanSpeechText, resolveProsody };
