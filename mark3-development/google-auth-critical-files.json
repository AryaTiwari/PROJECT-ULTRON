{
  "contractVersion": "google-auth-contract-v1",
  "runtime": "mark3",
  "criticalFiles": [
    "core/google-sheets-auth.js",
    "core/google-sheets-operator.js",
    "scripts/google-sheets-auth.js",
    "scripts/google-sheets-auth-doctor.js",
    "scripts/google-sheets-live-health.js",
    "scripts/google-auth-integrity-guard.js",
    "server.js",
    "interface/app.js",
    "interface/style.css",
    "scripts/google-sheets-auth-resilience-selftest.js",
    "core/apollo-lead-domain-controller.js",
    "core/apollo-lead-mission-store.js",
    "core/apollo-lead-mission-runner.js"
  ],
  "guarantees": [
    "stable project-root credential paths",
    "refresh token preservation",
    "atomic token writes and backup recovery",
    "single forced refresh on HTTP 401",
    "single-flight loopback PKCE reauthorization when durable auth is unrecoverable",
    "no tracked OAuth secrets",
    "browser-launch fallback keeps the loopback PKCE flow alive and surfaces a safe authorization URL in Live Activity",
    "semantic Google auth events cross SSE without access tokens, refresh tokens, or OAuth client secrets",
    "developer CLI prints the safe authorization URL when automatic browser launch fails",
    "stored OAuth client identity is validated before token reuse",
    "explicitly incompatible Google Sheets scopes require renewed consent",
    "Google Sheet preflight occurs before Apollo paid approval/search can start",
    "Apollo company candidates are checkpointed before Google Sheet projection",
    "mission intent and Sheet target remain durable across auth interruption",
    "runtime restart never silently replays an approval-scoped paid Apollo operation"
  ]
}
