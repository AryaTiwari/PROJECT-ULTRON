'use strict';
const fs=require('fs'),path=require('path'),crypto=require('crypto'),config=require('./config');
const DEFAULT_DIR=path.join(config.projectRoot,'.ultron','universal-enrichment'),FILE=process.env.ULTRON_M3_ENRICHMENT_MISSION_STORE?path.resolve(process.env.ULTRON_M3_ENRICHMENT_MISSION_STORE):path.join(DEFAULT_DIR,'missions.json'),DIR=path.dirname(FILE),BACKUP=FILE+'.bak';
const RESUMABLE=new Set(['INSPECTING','AWAITING_APOLLO_APPROVAL','READY','RUNNING','WAITING_PROVIDER','WAITING_PHONE_CALLBACKS','WAITING_BUDGET','PAUSED','INTERRUPTED','PARTIAL','PARTIAL_BUDGET_EXHAUSTED','FAILED_SAFE']);
const now=()=>new Date().toISOString();
function empty(){return{version:1,missions:[]};}
function read(file=FILE){try{const parsed=JSON.parse(fs.readFileSync(file,'utf8'));return{version:1,missions:Array.isArray(parsed.missions)?parsed.missions:[]};}catch{return null;}}
function load(){return read(FILE)||read(BACKUP)||empty();}
function save(state){fs.mkdirSync(DIR,{recursive:true});const temp=`${FILE}.${process.pid}.${Date.now()}.tmp`,payload=JSON.stringify({version:1,missions:(state.missions||[]).slice(-60)},null,2);fs.writeFileSync(temp,payload,{mode:0o600});JSON.parse(fs.readFileSync(temp,'utf8'));if(fs.existsSync(FILE))fs.copyFileSync(FILE,BACKUP);if(fs.existsSync(FILE))fs.rmSync(FILE,{force:true});fs.renameSync(temp,FILE);try{fs.chmodSync(FILE,0o600);}catch{}return state;}
function create(input={}){const state=load();if(input.requestKey){const existing=[...state.missions].reverse().find(m=>m.requestKey===input.requestKey&&m.completionState!=='COMPLETE');if(existing)return existing;}const at=now(),mission={requestKey:input.requestKey||null,missionId:`enrich-${Date.now()}-${crypto.randomBytes(4).toString('hex')}`,provider:input.provider||'apollo',spreadsheetId:input.spreadsheetId||null,spreadsheetUrl:input.spreadsheetUrl||null,spreadsheetTitle:input.spreadsheetTitle||null,sheetName:input.sheetName||null,sheetId:input.sheetId??null,schemaFingerprint:input.schemaFingerprint||null,requestedPOCs:input.requestedPOCs||[],requestedFields:input.requestedFields||[],ignoredFields:input.ignoredFields||[],readScope:input.readScope||[],writeScope:input.writeScope||null,protectedColumns:input.protectedColumns||[],status:input.status||'INSPECTING',createdAt:at,updatedAt:at,startedAt:null,completedAt:null,totalEligibleRows:Number(input.totalEligibleRows||0),rowsProcessed:0,rowsRemaining:Number(input.totalEligibleRows||0),rowCheckpoints:{},apolloCalls:0,apolloCacheHits:0,apolloDiscoveryCalls:0,apolloHydrations:0,apolloPhoneReveals:0,aiAttempts:0,providerState:{},lastSafeCheckpoint:null,completionState:'INSPECTING',approvalId:null,approvalValid:false,nextEligibleAt:null,nextEligibleAtSource:null,apolloUsageLedger:{discoveryCalls:0,discoveryCacheHits:0,personHydrations:0,hydrationCacheHits:0,emailRequests:0,phoneReveals:0,phoneRevealFallbacks:0,failedCalls:0,rateLimits:0,estimatedCreditsConsumed:null},budget:input.budget||{},provenance:[],request:input.request||null,lastError:null};state.missions.push(mission);save(state);return mission;}
function update(id,patch={}){const state=load(),index=state.missions.findIndex(m=>m.missionId===id);if(index<0)return null;const current=state.missions[index],next={...current,...patch,providerState:{...(current.providerState||{}),...(patch.providerState||{})},apolloUsageLedger:{...(current.apolloUsageLedger||{}),...(patch.apolloUsageLedger||{})},updatedAt:now()};state.missions[index]=next;save(state);return next;}
function get(id){return load().missions.find(m=>m.missionId===id)||null;}
function list(limit=20){return load().missions.slice(-Math.max(1,limit)).reverse();}
function latest(){return list(1)[0]||null;}
function byRequestKey(key){return list(60).find(m=>m.requestKey===key)||null;}
function latestResumable(){return list(60).find(m=>RESUMABLE.has(m.status)&&m.completionState!=='COMPLETE')||null;}
function checkpointRow(id,rowNumber,data={}){const mission=get(id);if(!mission)return null;const rows={...(mission.rowCheckpoints||{})},key=String(Number(rowNumber));rows[key]={...(rows[key]||{}),...data,rowNumber:Number(rowNumber),updatedAt:now()};const processed=Object.values(rows).filter(r=>['COMPLETE','NO_DATA','NO_VERIFIED_PERSON','EMAIL_NOT_AVAILABLE','PHONE_NOT_AVAILABLE','EMPLOYER_MISMATCH','APOLLO_EXHAUSTED','LINKEDIN_EXHAUSTED'].includes(r.state)||r.processed).length;return update(id,{rowCheckpoints:rows,rowsProcessed:processed,rowsRemaining:Math.max(0,Number(mission.totalEligibleRows||0)-processed),lastSafeCheckpoint:{rowNumber:Number(rowNumber),at:now(),state:rows[key].state||'COMPLETE'}});}
function recordProvenance(id,entries=[]){const mission=get(id);if(!mission)return null;const prior=Array.isArray(mission.provenance)?mission.provenance:[];return update(id,{provenance:[...prior,...entries].slice(-20000)});}function recordCommit(id,entries=[]){const state=load(),index=state.missions.findIndex(m=>m.missionId===id);if(index<0)return null;const mission=state.missions[index],rows={...(mission.rowCheckpoints||{})},at=now();for(const entry of entries){const key=String(Number(entry.rowNumber)),prior=rows[key]||{},fields={...(prior.fields||{}),[`${entry.pocOrdinal||0}:${entry.field}`]:{state:'FOUND',source:entry.source,verified:true}};rows[key]={...prior,rowNumber:Number(entry.rowNumber),state:prior.state||'PARTIAL',processed:Boolean(prior.processed),fields,updatedAt:at};}const processed=Object.values(rows).filter(r=>['COMPLETE','NO_DATA','NO_VERIFIED_PERSON','EMAIL_NOT_AVAILABLE','PHONE_NOT_AVAILABLE','EMPLOYER_MISMATCH','APOLLO_EXHAUSTED','LINKEDIN_EXHAUSTED'].includes(r.state)||r.processed).length,last=entries.at(-1);const next={...mission,rowCheckpoints:rows,rowsProcessed:processed,rowsRemaining:Math.max(0,Number(mission.totalEligibleRows||0)-processed),provenance:[...(Array.isArray(mission.provenance)?mission.provenance:[]),...entries].slice(-20000),lastSafeCheckpoint:last?{rowNumber:Number(last.rowNumber),at,state:rows[String(Number(last.rowNumber))]?.state||'COMPLETE'}:mission.lastSafeCheckpoint,updatedAt:at};state.missions[index]=next;save(state);return next;}
function markInterruptedOnStartup(){const state=load();let changed=false;state.missions=state.missions.map(m=>{if(m.status==='RUNNING'){changed=true;return{...m,status:'INTERRUPTED',completionState:'INTERRUPTED',approvalValid:false,updatedAt:now()};}return m;});if(changed)save(state);return changed;}
module.exports={DIR,FILE,BACKUP,RESUMABLE,load,save,create,update,get,list,latest,byRequestKey,latestResumable,checkpointRow,recordProvenance,recordCommit,markInterruptedOnStartup};
