const fs=require('fs');const path=require('path');const config=require('./config');const integrations=require('./integrations');const{appendJsonl,readJsonl}=require('./persistence');const KNOWLEDGE_PATH=path.join(config.root,'core','model-knowledge.json');function loadKnowledge(){try{const data=JSON.parse(fs.readFileSync(KNOWLEDGE_PATH,'utf8'));return Array.isArray(data.models)?data.models:[]}catch{return[]}}const KNOWLEDGE=loadKnowledge();function record(event={}){appendJsonl(config.performancePath,{provider:event.provider||'openrouter',model:event.model||'unknown',taskType:event.taskType||'general',success:Boolean(event.success),latencyMs:Number(event.latencyMs)||null,qualityScore:Number.isFinite(Number(event.qualityScore))?Number(event.qualityScore):null,reason:event.reason||null,at:new Date().toISOString()})}function history(){return readJsonl(config.performancePath)}function summarize(taskType=null){const rows=history().filter(row=>!taskType||row.taskType===taskType);const groups=new Map();for(const row of rows){const key=`${row.provider}::${row.model}`,group=groups.get(key)||{provider:row.provider,model:row.model,attempts:0,successes:0,latency:[],quality:[]};group.attempts++;if(row.success)group.successes++;if(Number.isFinite(row.latencyMs))group.latency.push(row.latencyMs);if(Number.isFinite(row.qualityScore))group.quality.push(row.qualityScore);groups.set(key,group)}return[...groups.values()].map(group=>({provider:group.provider,model:group.model,attempts:group.attempts,successRate:group.attempts?group.successes/group.attempts:0,averageLatencyMs:group.latency.length?group.latency.reduce((a,b)=>a+b,0)/group.latency.length:null,averageQuality:group.quality.length?group.quality.reduce((a,b)=>a+b,0)/group.quality.length:null}))}function isDevinModel(id){return integrations.isDevinModel(id)}function isRoutingAlias(id){const value=String(id||'').trim().toLowerCase();return value==='openrouter/free'||value==='openrouter/auto'}function isBigPickle(id){return integrations.isBigPickle(id)}function isAssistantEligibleModel(id){return integrations.isDirectProviderModel(id)}function providerFromModel(id){return integrations.providerFromModel(id)}function knowledgeFor(modelId){const value=String(modelId||'').trim().toLowerCase();if(!value)return null;return KNOWLEDGE.find(entry=>Array.isArray(entry.patterns)&&entry.patterns.some(pattern=>value===String(pattern).toLowerCase()||value.includes(String(pattern).toLowerCase())))||null}function suitability(modelId,taskType='general'){const entry=knowledgeFor(modelId);if(!entry)return{score:0,reason:'No curated profile; rely on live OpenRouter performance history.'};const task=String(taskType||'general').toLowerCase(),strengths=(entry.strengths||[]).map(String).map(v=>v.toLowerCase()),map={coding:['coding','tool use','agents'],debugging:['coding','reasoning','tool use','agents'],research:['reasoning','long context','general chat'],multimodal:['multimodal','vision','video','image'],planning:['planning','reasoning','agents','tool calling'],general:['general assistant','general intelligence','reasoning','chat']},wanted=map[task]||map.general,hits=wanted.filter(keyword=>strengths.some(strength=>strength.includes(keyword)||keyword.includes(strength)));return{score:Math.min(1,hits.length/Math.max(1,wanted.length)),reason:entry.speciality,strengths:entry.strengths}}async function catalog(){try{const payload=await integrations.models();const entries=integrations.payloadModelEntries(payload).filter(entry=>integrations.isDirectProviderModel(entry.id));const eligible=config.freeOnly?entries.filter(integrations.isFreeModelEntry):entries;if(!eligible.length)throw new Error('OpenRouter returned no eligible models.');const enriched=eligible.map(entry=>{const profile=knowledgeFor(entry.id),fit=suitability(entry.id,'general');return profile?{model:entry.id,provider:'openrouter',...profile,suitability:fit.score,free:integrations.isFreeModelEntry(entry)}:{model:entry.id,provider:'openrouter',suitability:fit.score,free:integrations.isFreeModelEntry(entry)}}),providerCounts={openrouter:enriched.length};return{models:eligible.map(e=>e.id),enriched,count:eligible.length,rawCount:integrations.payloadModels(payload).length,devinExcludedCount:0,providerCounts,knowledgeProfilesMatched:enriched.filter(item=>item.speciality).length,source:'openrouter-live-model-catalog'}}catch(error){throw new Error(`OpenRouter model catalog unavailable: ${error instanceof Error?error.message:String(error)}`)}}async function intelligence(taskType=null){const live=await catalog(),observed=summarize(taskType),enriched=live.enriched.map(item=>({...item,suitability:suitability(item.model,taskType||'general').score,observed:observed.find(row=>row.model===item.model)||null}));return{live:{...live,enriched},observed,generatedAt:new Date().toISOString()}}module.exports={record,history,summarize,catalog,intelligence,loadKnowledge,knowledgeFor,suitability,isRoutingAlias,isDevinModel,isBigPickle,isAssistantEligibleModel,providerFromModel};