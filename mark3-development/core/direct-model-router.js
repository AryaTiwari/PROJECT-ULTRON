const { load: loadCredentials } = require('./credentials/local-store');
const PROVIDERS={gemini:{key:'GEMINI_API_KEY',family:'gemini',defaultModel:'gemini-2.5-flash',baseUrl:'https://generativelanguage.googleapis.com/v1beta'},openai:{key:'OPENAI_API_KEY',family:'openai',defaultModel:'gpt-5.4-mini',baseUrl:'https://api.openai.com/v1'},anthropic:{key:'ANTHROPIC_API_KEY',family:'anthropic',defaultModel:'claude-sonnet-4-5',baseUrl:'https://api.anthropic.com/v1'},deepseek:{key:'DEEPSEEK_API_KEY',family:'openai-compatible',defaultModel:'deepseek-chat',baseUrl:'https://api.deepseek.com'},groq:{key:'GROQ_API_KEY',family:'openai-compatible',defaultModel:'llama-3.3-70b-versatile',baseUrl:'https://api.groq.com/openai/v1'},mistral:{key:'MISTRAL_API_KEY',family:'openai-compatible',defaultModel:'mistral-small-latest',baseUrl:'https://api.mistral.ai/v1'},xai:{key:'XAI_API_KEY',family:'openai-compatible',defaultModel:'grok-4',baseUrl:'https://api.x.ai/v1'},openrouter:{key:'OPENROUTER_API_KEY',family:'openai-compatible',defaultModel:'openrouter/free',baseUrl:'https://openrouter.ai/api/v1'},nvidia:{key:'NVIDIA_API_KEY',family:'openai-compatible',defaultModel:'nemotron-3.5-lightning-30b-a3b',baseUrl:'https://integrate.api.nvidia.com/v1'},opencode:{key:'OPENCODE_API_KEY',family:'openai-compatible',defaultModel:'big-pickle',baseUrl:'https://opencode.ai/zen/v1'},'opencode-go':{key:'OPENCODE_GO_API_KEY',family:'openai-compatible',defaultModel:'kimi-k3',baseUrl:'https://opencode.ai/zen/go/v1'}};
const FREE_OPENCODE_MODELS=['big-pickle','mimo-v2.5-free','hy3-free','nemotron-3-ultra-free','nemotron-3.5-lightning-free','x-preview-f-free','muse-spark-1.2-contributor-free'];
function parseModel(model){const value=String(model||'auto').trim();if(!value||value==='auto')return{provider:null,model:'auto'};const slash=value.indexOf('/');if(slash<0)return{provider:null,model:value};return{provider:value.slice(0,slash).toLowerCase(),model:value.slice(slash+1)};}function providerForModel(model){const{provider}=parseModel(model);if(provider&&PROVIDERS[provider])return provider;const lower=String(model||'').toLowerCase();if(FREE_OPENCODE_MODELS.includes(lower)||lower==='big-pickle'||lower==='glm-5')return'opencode';if(lower==='kimi-k3')return'opencode-go';if(/^(gemini|models\/gemini|gg\/)/.test(lower))return'gemini';if(/^(gpt|o[134]|openai|oa\/)/.test(lower))return'openai';if(/^(claude|anthropic|cc\/)/.test(lower))return'anthropic';if(/^deepseek|^ds\//.test(lower))return'deepseek';if(/^llama|^qwen|^groq\//.test(lower))return'groq';if(/^mistral/.test(lower))return'mistral';if(/^grok|^xai\//.test(lower))return'xai';if(/^openrouter\//.test(lower))return'openrouter';if(/^nvidia\//.test(lower))return'nvidia';return null;}
async function credentials(){const out={};try{const stored=await loadCredentials();for(const[p,cfg]of Object.entries(PROVIDERS))out[p]=String(stored[cfg.key]||process.env[cfg.key]||'').trim();if(!out.nvidia)out.nvidia=String(stored.NVIDIA_NIM_API_KEY||process.env.NVIDIA_NIM_API_KEY||'').trim();const aliases={gemini:'GOOGLE_API_KEY',openai:'OPENAI_API_KEY',anthropic:'ANTHROPIC_API_KEY'};for(const[p,k]of Object.entries(aliases))if(!out[p])out[p]=String(process.env[k]||'').trim();}catch{for(const[p,cfg]of Object.entries(PROVIDERS))out[p]=String(process.env[cfg.key]||'').trim();if(!out.nvidia)out.nvidia=String(process.env.NVIDIA_NIM_API_KEY||'').trim();}return out;}
async function chooseAutoModel(){const creds=await credentials();const envAuto=String(process.env.ULTRON_DIRECT_DEFAULT_MODEL||'').trim();if(envAuto){const p=providerForModel(envAuto);if(p&&creds[p])return envAuto;}for(const p of['gemini','openai','anthropic','nvidia','deepseek','groq','mistral','xai'])if(creds[p])return`${p}/${process.env[`ULTRON_DIRECT_${p.toUpperCase()}_MODEL`]||PROVIDERS[p].defaultModel}`;throw new Error('No direct AI provider credential is configured. Add a provider key in ULTRON ACCESS or environment.');}
function normalizeMessages(messages){return(Array.isArray(messages)?messages:[]).map(m=>({role:m.role==='model'?'assistant':m.role,content:typeof m.content==='string'?m.content:String(m.content??'')}));}
async function directChat({messages,model='auto',tools=null}={}){if(!Array.isArray(messages)||!messages.length)throw new Error('Model request requires messages.');const resolved=model==='auto'?await chooseAutoModel():model;const initial=parseModel(resolved);const provider=initial.provider&&PROVIDERS[initial.provider]?initial.provider:providerForModel(resolved);if(!provider||!PROVIDERS[provider])throw new Error(`Unsupported direct model: ${resolved}`);const cfg=PROVIDERS[provider];const creds=await credentials();const apiKey=creds[provider];if(!apiKey)throw new Error(`Provider ${provider} is not configured locally.`);const cleanMessages=normalizeMessages(messages);const attempt=async(attemptModel)=>{const{model:parsedModel}=parseModel(attemptModel);let response;if(cfg.family==='gemini'){const system=cleanMessages.find(m=>m.role==='system')?.content||'';const contents=cleanMessages.filter(m=>m.role!=='system').map(m=>({role:m.role==='assistant'?'model':'user',parts:[{text:m.content}]}));const body={contents};if(system)body.systemInstruction={parts:[{text:system}]};response=await fetch(`${cfg.baseUrl}/models/${encodeURIComponent(parsedModel||cfg.defaultModel)}:generateContent?key=${encodeURIComponent(apiKey)}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const raw=await response.text();let data={};try{data=raw?JSON.parse(raw):{}}catch{}if(!response.ok){const e=new Error(`Gemini HTTP ${response.status}: ${raw.slice(0,800)}`);e.status=response.status;throw e;}const content=data?.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('')||'';if(!content.trim())throw new Error('Gemini returned no response text.');return{content,toolCalls:[],model:parsedModel||cfg.defaultModel,provider,raw:data};}if(cfg.family==='anthropic'){const system=cleanMessages.find(m=>m.role==='system')?.content||'';const msgs=cleanMessages.filter(m=>m.role!=='system').map(m=>({role:m.role==='assistant'?'assistant':'user',content:m.content}));const body={model:parsedModel||cfg.defaultModel,max_tokens:Number(process.env.ULTRON_ANTHROPIC_MAX_TOKENS||2048),messages:msgs};if(system)body.system=system;response=await fetch(`${cfg.baseUrl}/messages`,{method:'POST',headers:{'Content-Type':'application/json','x-api-key':apiKey,'anthropic-version':'2023-06-01'},body:JSON.stringify(body)});const raw=await response.text();let data={};try{data=raw?JSON.parse(raw):{}}catch{}if(!response.ok){const e=new Error(`Anthropic HTTP ${response.status}: ${raw.slice(0,800)}`);e.status=response.status;throw e;}const content=Array.isArray(data?.content)?data.content.filter(x=>x.type==='text').map(x=>x.text).join(''):'';if(!content.trim())throw new Error('Anthropic returned no response text.');return{content,toolCalls:[],model:data?.model||parsedModel||cfg.defaultModel,provider,raw:data};}response=await fetch(`${cfg.baseUrl}/chat/completions`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${apiKey}`},body:JSON.stringify({model:parsedModel||cfg.defaultModel,messages:cleanMessages,...(Array.isArray(tools)&&tools.length?{tools}: {})})});const raw=await response.text();let data={};try{data=raw?JSON.parse(raw):{}}catch{}if(!response.ok){const e=new Error(`${provider} HTTP ${response.status}: ${raw.slice(0,800)}`);e.status=response.status;e.raw=raw;throw e;}const message=data?.choices?.[0]?.message||{};const content=message.content??data?.choices?.[0]?.text??data?.output_text??'';const toolCalls=Array.isArray(message.tool_calls)?message.tool_calls:[];if(!String(content).trim()&&!toolCalls.length)throw new Error(`${provider} returned no response text or tool calls.`);return{content:String(content||''),toolCalls,model:data?.model||parsedModel||cfg.defaultModel,provider,raw:data};};if(provider==='opencode'&&(!initial.model||FREE_OPENCODE_MODELS.includes(initial.model))){const start=Math.max(0,FREE_OPENCODE_MODELS.indexOf(initial.model));const ordered=[...FREE_OPENCODE_MODELS.slice(start),...FREE_OPENCODE_MODELS.slice(0,start)];let lastError;for(const candidate of ordered){try{return await attempt(`opencode/${candidate}`)}catch(error){lastError=error;if(![408,425,429,500,502,503,504].includes(Number(error.status)))break;}}throw lastError||new Error('OpenCode free model pool is unavailable.');}try{return await attempt(resolved);}catch(error){if(provider==='nvidia'&&[401,403,410].includes(Number(error.status))){const candidates=['gemini','openai','anthropic','deepseek','groq','mistral','xai'];for(const p of candidates){if(!creds[p]||p==='nvidia')continue;try{return await attempt(`${p}/${PROVIDERS[p].defaultModel}`);}catch{}}}throw error;}}
async function health(){const creds=await credentials();const configured=Object.fromEntries(Object.keys(PROVIDERS).map(id=>[id,Boolean(creds[id])]));return{direct:true,providers:configured,anyConfigured:Object.values(configured).some(Boolean)};}
module.exports={PROVIDERS,FREE_OPENCODE_MODELS,directChat,health,chooseAutoModel,providerForModel,parseModel};